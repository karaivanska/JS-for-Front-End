/*
You are given a number N. Create a JS function that multiplies the number by 2 and prints the result. The input
comes as an array of strings.
 */

"use strict";

function multiply(n) {
    return n * 2;
}
console.log(multiply(3));